#!/usr/bin/env python3
"""CLI entrypoint. Kept import-light so `dispatch status` is instant."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from dispatch.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
